Draft README

Backend
// the methods to handle requests (GET, POST, PUT, DELETE...) are defined in controllers
// the routes are defined in routes
// the routes are imported into server.js and used as middleware
// the server is started in server.js